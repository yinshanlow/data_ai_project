anthropic==0.40.0
pandas==2.2.3
requests==2.32.3
streamlit==1.40.2
fastapi==0.115.5
uvicorn==0.32.1
python-dotenv==1.0.1
pydantic==2.10.2

# Only needed for the embedded policy-pipeline fallback (see tools/policy.py) —
# used when genai-power-platform-agent's own FastAPI service isn't reachable,
# which is always the case on Streamlit Community Cloud (one process per app).
langchain==0.3.7
langchain-community==0.3.7
langchain-chroma==0.1.4
langchain-huggingface==0.1.2
chromadb==0.5.20
pysqlite3-binary; sys_platform == "linux"
sentence-transformers==3.3.1
