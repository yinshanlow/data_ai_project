# Core (used by every script)
pandas>=2.1
numpy>=1.26

# ETL - production-equivalent layer (etl/transform_pyspark.py)
# Requires Java 8/11/17 installed locally. On Databricks Community Edition,
# skip this - PySpark is already available in the notebook environment.
pyspark>=3.5

# ML layer (ml/train_churn_model.py)
scikit-learn>=1.3
joblib>=1.3

# Dashboard (dashboard/app.py)
streamlit>=1.32
plotly>=5.20
