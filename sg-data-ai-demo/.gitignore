# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Spark
data/processed/customer_features/
*.crc
_SUCCESS

# ML artifacts (regenerated by train_churn_model.py - kept out of git so the
# repo stays small; comment these out if you'd rather commit them for
# reviewers who don't want to run the pipeline themselves)
# ml/artifacts/churn_model.pkl

# OS / editor
.DS_Store
.vscode/
.idea/
